name: Publish NuGet SqlSensei.Core.EndpointLogger

on:
    workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.x'

    - name: Build and package SqlSensei.Core.EndpointLogger
      run: dotnet build src/SqlSensei.Core.EndpointLogger/SqlSensei.Core.EndpointLogger.csproj --configuration Release
      run: dotnet pack src/SqlSensei.Core.EndpointLogger/SqlSensei.Core.EndpointLogger.csproj --configuration Release

    - name: Publish NuGet SqlSensei.Core.EndpointLogger
      run: dotnet nuget push src/SqlSensei.Core.EndpointLogger/bin/Release/*.nupkg
      env:
        NUGET_API_KEY: ${{ secrets.NUGET_PUBLISH_API_KEY }}
